version: "3.9"

services:
  bot:
    build: .
    container_name: gptv-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./database:/app/database
      - ./logs:/tmp
    environment:
      - DEBUG=False
      - TZ=UTC
